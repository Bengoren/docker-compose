version: 3.8

sevices:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      container_name: multistage-food
    ports:
      - "5000:5000"
  nginx:
    image: nginx:latest
    container_name: nginx-reverse-proxy
    depends_on:
      - web
    ports:
      - "80:80"
